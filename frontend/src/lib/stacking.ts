export interface StackingInfo {
  stxAmount: bigint;
  btcAmount: bigint;
  cycle: number;
}

export async function stakeDual(stxAmount: bigint, btcAmount: bigint) {
  // Implementation for dual stacking
  return { success: true };
}
